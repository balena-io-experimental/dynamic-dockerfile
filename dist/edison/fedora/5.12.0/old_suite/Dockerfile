
# AUTOGENERATED FILE
# Resin.io base image for Intel Edison, Fedora, node v5.12.0, variant: old_suite
FROM resin/edison-fedora-buildpack-deps:23

ENV NODE_VERSION 5.12.0

RUN curl -SLO "http://nodejs.org/dist/v5.12.0/node-v5.12.0-linux-i386.tar.gz" \
	&& echo "" | sha256sum -c - \
	&& tar -xzf "node-v5.12.0-linux-i386.tar.gz" -C /usr/local --strip-components=1 \
	&& rm "node-v5.12.0-linux-i386.tar.gz" \
	&& npm config set unsafe-perm true -g --unsafe-perm \
	&& rm -rf /tmp/*

CMD ["echo","'No CMD command was set in Dockerfile! Details about CMD command could be found in Dockerfile Guide section in our Docs. Here's the link: http://docs.resin.io/#/pages/using/dockerfile.md"]