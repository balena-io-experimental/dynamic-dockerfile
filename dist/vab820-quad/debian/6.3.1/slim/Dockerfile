
# AUTOGENERATED FILE
# Resin.io base image for VIA VAB 820-quad, Debian, node v6.3.1, variant: slim
FROM resin/vab820-quad-debian:latest

ENV NODE_VERSION 6.3.1

RUN buildDeps='curl' \
	&& set -x \
	&& apt-get update && apt-get install -y $buildDeps --no-install-recommends \
	&& rm -rf /var/lib/apt/lists/* \
	&& curl -SLO "{ $binary_url }}" \
	&& echo "" | sha256sum -c - \
	&& tar -xzf "node-v6.3.1-linux-armv7l.tar.gz" -C /usr/local --strip-components=1 \
	&& rm "node-v6.3.1-linux-armv7l.tar.gz" \
	&& apt-get purge -y --auto-remove $buildDeps \
	&& npm config set unsafe-perm true -g --unsafe-perm \
	&& rm -rf /tmp/*

CMD ["echo","'No CMD command was set in Dockerfile! Details about CMD command could be found in Dockerfile Guide section in our Docs. Here's the link: http://docs.resin.io/#/pages/using/dockerfile.md"]